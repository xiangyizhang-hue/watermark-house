# 前台版候选包验收 — 2026-09-20 晚

按用户确认的收敛方案执行：暂缓独立后台渲染引擎。当前不是最终交付包，不提供给用户反复试装。

## 候选版本

- 构建：`scripts/android-local-test.ps1 -Architecture x86_64`，离线内置前端。
- APK：`src-tauri/gen/android/app/build/outputs/apk/x86_64/debug/app-x86_64-debug.apk`。
- SHA256：`E00DF2783A369DBFBA152F1A11977572CD573E48F327EE657A113D82EBE57653`。
- 设备：现有 Android15 x86_64 模拟器 emulator-5554；不是 vivo X100s，也不是手机可用的arm64交付包。
- TypeScript/生产构建通过；45文件362项自动测试通过。旧Canvas回退测宽测试不作为视觉证据。

## 修复

- 批次“全部尝试过”不再等同“全部保存成功”。失败项即使位于游标之前也保留并重试，只有每张输出都有成功记录才删除队列。
- 原生删除队列失败时，保留界面中的恢复入口。
- 恢复卡显示实际已保存数，不拿尝试游标冒充成功数。
- 手机端切后台请求取消当前批量循环，返回后显式继续；等待通知服务启动，避免启动与取消标识重置竞态。
- 按UI/UX技能的就地反馈规范增加前台/亮屏限制说明，没有重做设计系统。

## 实际通过

1. 覆盖安装成功，关闭Wi-Fi和移动网络后冷启动。URL为内置`http://tauri.localhost/`，不是5180；启动错误数组为空。
2. 覆盖安装后原有1200×800测试照片和“X100s 离线水印恢复测试”水印保留，JPG写入相册，90858字节。截图实见上下品牌和水印。
3. 安卓返回键关闭导出预览，仍停留在导出页。导出页无横向溢出，按钮不再被固定高度裁切。
4. 用冻结队列中的非法裁剪制造真实渲染失败：显示失败1、已保存0/1；原生SQLite队列保留为paused/cursor0。只修正测试队列参数、不改编辑历史后重试成功，实际相册文件生成、队列变为null。
5. 通过真实file input change链路导入6000×4000、800×1200、1000×1000合成照片；不是系统相册选择器测试。
6. 2400万像素合成照片JPG和PNG单张均成功保存为6000×4000，无静默降尺寸。合成图压缩率高，不代表复杂照片或更大批次性能。
7. 混合四张PNG批量全部成功。拉取实际文件并读取PNG头：1200×800、6000×4000、800×1200、1000×1000均正确；队列清空。新方图成片只含合成原图内容，无额外水印或边框。
8. 再次四张批量中按安卓HOME键：返回后原生队列paused/cursor1、已保存1/4。点击继续后队列清空；相册中同一批`2b22721b4c`四个文件齐全，第一张仅有一份。

证据：`.tools/framelab-candidate-export.png`、`.tools/framelab-candidate-batch.png`、`.tools/candidate-album/`。测试图片内部文字和白色定位矩形是合成原图本身，不是应用默认装饰。

故障注入备注：最初尝试替换只读的Tauri invoke函数未生效（injected=false），该次运行仅计正常批量导出；失败恢复证明来自后续非法裁剪队列，不来自未生效的注入。

## 仍待验收/交付

模板跨图保存/恢复、全部编辑入口与对齐/色卡、撤销复位及每图隔离、系统选图器、横屏/大字体/键盘、PNG透明度、较大实际照片与批量负载、arm64安装包与长期签名升级。当前不能声称完整手机端已完成。
