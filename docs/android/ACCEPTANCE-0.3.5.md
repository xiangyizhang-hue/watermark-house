# 0.3.5 验收记录

环境：Windows本机，FrameLab_API35 Android15 x86_64模拟器。没有本轮vivo真机性能数据。

## 已通过

- Vue/TypeScript生产构建。
- Vitest：48文件、377测试全部通过，包含备份schema/取消/存储失败待重试、模板版本和缺失素材拒绝、删除确认、自定义Logo保存及重映射、快照幂等恢复。
- 原生隔离测试：备份追加恢复、原图逐字节相同、历史参数与撤销位置、Logo新标识映射、移除照片的残留历史排除、路径穿越/截断文件拒绝、取消恢复。测试使用独立临时数据库，不修改实际图库。
- 系统选择器实测：7张原图、13条历史、1模板创建ZIP，再追加恢复后14张、2模板，原有记录仍在。退出模拟器/重启应用后保留。
- 模板系统保存JSON，再系统选择器导入到“我的模板”，数量从2增至3，成功提示正确。
- 360/375/393/430宽编辑、水印面板、快捷文字、模板库，共16组合：容器无横向溢出，降低动画模式生效。
- 原生375宽、系统1.3倍字体、键盘弹出：品牌联动和输入可读，完成按钮可达。
- 原生横屏863×360：发现并修复画布底部裁切，修复后画布与右面板边界相接不重叠，完整照片位于工具栏之上。
- 手机APK：arm64-v8a、最低API31、目标API35、版本0.3.5/3005；签名与0.3.4一致；16KB ZIP对齐检查通过；无debuggable标志。
- 最终同源码x86_64 release安装成功，关闭模拟器Wi-Fi及数据后冷启动，14张图库保留。实际导出 `offline-landscape_1790092291011.jpg`：1200×800、90921字节，系统Pictures/FrameLab目录新增文件，界面显示导出成功。

最终手机APK SHA256：`B373C0D2DE9F69584828836CF18F454C0E0D37C636E8771ECA7CCAA828081EBA`。

最终模拟器release APK SHA256：`C39D5530174A9065F430CC7796583C2614F113B8CA45B0CBF90B326EF5424099`。两者均包含最终自定义Logo模板修正，不同ABI二进制哈希自然不同。

## 证据

工程内 `.tools/tests-035-final.log`、`.tools/ui035-width-check.json`、`.tools/build-035-final-arm64.log`、`.tools/build-035-delivery-x86.log`、`.tools/ui035-library.png`、`.tools/ui035-landscape-fit.png`、`.tools/ui035-text-large.png`、`.tools/final035-export-result.png`。交付目录附测试日志和主要截图。

## 不等同于已验收

长时间锁屏导出、64MP/100MP大批量、内存峰值/60fps、低存储及断电故障、所有桌面模式成片逐像素比较、Android12/14/16、无障碍读屏完整流程、vivo真机UI验收。备份并非加密，也不包含偏好设置及未完成导出任务。
